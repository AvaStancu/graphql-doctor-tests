# Define the types
type User {
  id: ID!
  username: String!
  email: String!
  age: Int
}

# Define the queries
type Query {
  users: [User!]!
  user(id: ID!): User
}

# Define the schema
schema {
  query: Query
}
