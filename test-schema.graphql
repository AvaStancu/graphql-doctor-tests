# Define the types
type User {
  id: Int!
  username: String!
  email: String!
  databaseId: Int
  someId: Int
}

# Define the queries
type Query {
  users: [User!]!
  user(id: ID!): User
}

# Define the schema
schema {
  query: Query
}
