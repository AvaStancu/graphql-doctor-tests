# Define the types
type User {
  id: ID!
  username: String!
  email: String!
  databaseId: Int
  someId: Int
  id: Int
}

# Define the queries
type Query {
  users: [User!]!
  user(id: ID!): User
}

# Define the schema
schema {
  query: Query
}
